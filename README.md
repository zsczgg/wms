# (주)보쥬

一个零依赖的轻量 WMS 系统，支持客户子账号、库存可视化、入库/退仓/代发申请和管理员处理流程。

## 启动

```bash
node server.js
```

打开：

- 统一入口：http://localhost:4173

默认账号：

- 管理员：`admin` / `admin123`
- 演示客户：`client` / `client123`

## 数据

数据保存在 `data/db.json`。首次启动会自动生成演示数据。
